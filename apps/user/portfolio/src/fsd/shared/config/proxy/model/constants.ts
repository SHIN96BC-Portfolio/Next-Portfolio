import { DEFAULT_LOCALE } from '@FsdShared/config/i18n/client';

/**
 * proxy 미들웨어·레거시 코드에서 쓰는 기본 locale alias.
 * `DEFAULT_LOCALE`(`ko`)와 동일한 값이며, 하위 호환을 위해 유지합니다.
 */
export const defaultLocale = DEFAULT_LOCALE;

export default undefined;
