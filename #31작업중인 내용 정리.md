# #31 작업 중인 내용 정리 (인수인계)

> **목적:** 다른 AI/사람이 이어서 작업할 수 있도록, 대화에서 합의·구현한 내용과 **다음에 할 일·지켜야 할 규칙**을 한곳에 모은 문서다.  
> **소스 오브 트루스:** 상세 규칙은 루트 [`README.md`](README.md) · 에이전트 요약은 [`AGENTS.md`](AGENTS.md). 충돌 시 README 우선.  
> **관련 대화 transcript:** Cursor agent transcript `b9c2bb2a-0143-4a27-9f6c-69c67d5fa0ad`  
> **작성 시점:** 2026-09 (작업 브랜치 이슈 #31 가정 — 브랜치 컨벤션 `{type}/{name}/#{issueNo}`)

---

## 1. 한 줄 요약

FSD 폴더·`_parts`·cross-slice import 규칙을 **문서 + Biome + 커스텀 lint**로 강제하는 작업을 진행 중이며,  
**다음 우선 작업**은 (1) entities same-slice alias→상대경로 정리 후속, (2) Layout header deep import, (3) [`SECURITY-NOTES.tmp.md`](SECURITY-NOTES.tmp.md) 기반 보안 구현이다.  
폴더 lint는 **이미 동작**하며, 일부 규칙은 **의도적으로 미구현**(model depth, shared/app/pages 등).

---

## 2. 배경 · 왜 이렇게 했는지 (의사결정)

### 2.1 `_parts`를 widgets만 쓰기로 한 이유

- FSD 계층(shared → entities → features → widgets → pages)이 **시나리오·도메인 단위로 쪼개져야** 찾기 쉽다.
- `_parts`가 features/entities에도 있고, widgets `_parts`가 비대해지면 **features로 가야 할 UI**가 widgets private에 묻힌다.
- 합의: **`_parts` = widgets public 1개의 얇은 private 조립만**. 시나리오급(설정·공유·로그인 등)은 **`features/<scenario>`**.

### 2.2 `ui/_parts` 공용 통 → component-root

- `widgets/header/ui/_parts`처럼 **ui 직하위 공용 `_parts`** 는 “어느 public 소속인지” 경로만으로 알기 어렵다.
- 신규 필수: `ui/<PublicComponent>/_parts/` (**component-root**).
- **레거시 예외 1곳만 허용:**  
  `apps/user/portfolio/src/fsd/widgets/header/ui/_parts`  
  → 마이그레이션 대상. **신규 `ui/_parts` 추가 금지.**

### 2.3 `_parts` import는 `./_parts/**`만

- 긴 상대경로/`../_parts`/alias로 `_parts`를 뚫으면 소유권·경계가 무너진다.
- Biome `noRestrictedImports`: `_parts` 포함 import **전부 금지**, 예외 **`!./_parts` / `!./_parts/**`만**.
- 외부는 widget **public** `ui/index.ts`(또는 public 컴포넌트)만 사용.

### 2.4 widgets/features alias 금지, entities는 다름

| 레이어 | same-slice | cross-slice |
|--------|------------|-------------|
| **widgets** | **상대경로만** (`@FsdWidgets/**` 금지) | 금지 |
| **features** | **상대경로만** (`@FsdFeatures/**` 금지) | 금지 |
| **entities** | alias(`@FsdEntities/**`) **허용**(전면 금지 없음). same-slice를 상대경로로 맞추는 작업은 **스타일/일관성** 목적 | 다른 entities 슬라이스 import는 FSD상 금지·리뷰 |

IDE “Import can be shortened”이 entities에서 뜨는 것은 **entities alias가 허용**이라 정상일 수 있다. widgets/features에서는 alias를 쓰면 Biome이 막는다.

### 2.5 `_parts` 안에서는 `_parts/_parts` 금지

- 역할 폴더만: `drawers` | `sheets` | `nav`
- `_parts`가 시나리오 이름으로 4~5개+ 쌓이면 → **features로 올리거나 widget 슬라이스 분리**

### 2.6 ui 형제 `.tsx` 분할 기준

- **세는 단위 = 같은 폴더 형제 `.tsx`** (`index.ts` 제외). 슬라이스 전체 TSX가 아님.
- **8개 이상** → PascalCase component-folder 또는 역할 폴더로 분할.
- 재귀 적용.

---

## 3. 이미 끝난 작업 (Done)

### 3.1 문서

- [`README.md`](README.md): `_parts` widgets 전용 · component-root · 배치 기준 · Biome/lint 강제 문구 반영.
- [`AGENTS.md`](AGENTS.md): `_parts` 요약 + `pnpm lint:fsd` / `RULES` object 안내.

### 3.2 Biome (`biome.json`)

- widgets / features: `@FsdWidgets/**` · `@FsdFeatures/**` 금지 (레이어별 메시지).
- 앱 전역(해당 override): `_parts` import는 `./_parts/**`만.
- entities: `@FsdEntities/**` **전면 금지는 하지 않음**.

### 3.3 커스텀 lint 스크립트

| 파일 | 역할 | npm 스크립트 |
|------|------|----------------|
| [`scripts/lint-fsd-folder-structure.mjs`](scripts/lint-fsd-folder-structure.mjs) | layer/slice/group 파일 금지, ui 형제 tsx 상한, depth, `_parts` 위치 등 — **상단 `RULES` object** | `pnpm lint:fsd` / `pnpm lint:fsd:dry` |
| [`scripts/lint-fsd-ui-private-folders.mjs`](scripts/lint-fsd-ui-private-folders.mjs) | widgets 밖 `_parts` 금지, ui 하위 폴더명, `_parts` 역할 폴더 | `pnpm biome` 체인에 포함 |

`package.json`:

```text
"biome": "biome check . && node scripts/lint-fsd-ui-private-folders.mjs && node scripts/lint-fsd-folder-structure.mjs"
"lint:fsd": "node scripts/lint-fsd-folder-structure.mjs"
"lint:fsd:dry": "node scripts/lint-fsd-folder-structure.mjs --dry-run"
```

- 주석: **한국어**로 정리해 둠 (다음 작업자도 RULES 주석을 한글로 유지).
- 마지막 확인 시 두 스크립트 모두 **passed**.

### 3.4 코드 쪽 부분 수정 (대화 중)

- `PortfolioCareer.tsx` 등에서 `header/ui/_parts/...` **deep import 제거** (규칙 위반 수정).
- content/site mock·mapper·lang 관련 변경이 **워킹트리에 다수** 있을 수 있음 (아래 §5 git 상태 참고) — **커밋은 사용자 요청 전까지 하지 말 것.**

### 3.5 임시 스크립트

- `scripts/tmp-convert-entities-imports.mjs` — same-slice `@FsdEntities` → 상대경로 일괄 변환용으로 **실행 후 삭제된 상태**여야 함. 필요 시 재작성.

---

## 4. lint 규칙 반영 매트릭스 (현재)

### 4.1 `lint-fsd-folder-structure.mjs` (`RULES`)

| 규칙 | 상태 | 비고 |
|------|------|------|
| layer root에 소스 파일 금지 | ✅ | widgets/features/entities |
| slice root에 소스 파일 금지 | ✅ | entities만 `allowIndex: true` |
| group root에 소스 파일 금지 | ✅ | 예: `widgets/portfolio/` |
| ui 형제 `.tsx` ≤ 7 (8+ 분할) | ✅ | `maxSiblingTsx: 7` |
| max depth from layer | ✅ | widgets **5**, features/entities **3** |
| `_parts` widgets만 | ✅ | features/entities `parts.enabled: false` |
| `ui/_parts` 공용 통 금지 | ✅ | legacy allowlist만 예외 |
| `_parts/_parts` 금지 | ✅ | |
| `_parts` 역할 폴더 drawers\|sheets\|nav | ✅ | 비표준은 warn |
| private `_` 폴더명 `_parts`만 | ✅ | widgets ui walk |
| **model/** 형제·depth | ❌ | 주석에 “추후 RULES 추가” |
| 슬라이스명 kebab-case 강제 | ❌ | |
| 세그먼트 allowlist 강제 | △ | 비표준은 **warn**만 |
| shared / app / pages | ❌ | `structuredLayers`에 미포함 |

### 4.2 `lint-fsd-ui-private-folders.mjs`

| 규칙 | 상태 |
|------|------|
| features/entities/pages/app/shared에 `_parts` 금지 | ✅ |
| `ui/_parts` 공용 통 금지 (legacy 예외) | ✅ |
| `_parts/_parts` 금지 | ✅ |
| 역할 폴더 / PascalCase만 | ✅ |
| `_` prefix 임의 폴더 금지 | ✅ |

### 4.3 중복 주의

- `_parts` 위치·역할 검사가 **두 스크립트에 겹침**.  
  장기적으로 `lint-fsd-folder-structure.mjs`로 **통합**하고 private-folders를 제거하는 방안을 논의했으나 **아직 미실행**.  
  통합 시 legacy allowlist·메시지를 한곳으로 모을 것.

---

## 5. 다음에 해야 할 작업 (우선순위)

대화에서 합의한 **순서대로** 진행:

### P0 — 이어가기 (원래 3대 과제)

1. **entities same-slice alias → 상대경로 일괄화**
   - 목적: widgets/features와 같이 same-slice는 상대경로로 읽기 쉽게 (entities는 Biome 전면 금지는 **하지 않기로** 한 상태).
   - 이미 tmp 스크립트로 일부/전부 돌렸을 수 있음 → **현재 diff·grep으로 잔여 `@FsdEntities/` same-slice 확인** 후 마무리.
   - cross-slice `@FsdEntities/other-slice`는 FSD 위반이므로 **상대경로로 “고치는” 게 아니라 구조/공유 타입을 shared로** 검토.

2. **Layout header deep import**
   - 예: `@FsdWidgets/header/ui/PortfolioHeader` 같은 **public이 아닌 deep path** 또는 layout에서 widgets 내부 경로 직접 import.
   - 올바른 패턴: widgets 슬라이스 **public API** (`ui/index.ts` 등)만 상위(pages/app/layout)에서 import.
   - `_parts`는 절대 layout/다른 widget에서 import하지 않음.

3. **보안 구현** — [`SECURITY-NOTES.tmp.md`](SECURITY-NOTES.tmp.md)
   - **아직 코드 구현 거의 안 함**(문서만). 임시 메모이므로 구현 범위는 문서의 “당장 고칠지 / 장기”를 읽고 **사용자와 범위 확인** 후 착수.
   - 핵심 논점 요약: `NEXT_PUBLIC_*` ≠ secret, 클라이언트 crypto 한계, XSS vs JS의 accessToken, next-auth 세션에 토큰 노출, HttpOnly/BFF 경계 등.
   - 구현 시 **시크릿을 커밋하지 말 것**. env 예시는 `.env.example` 수준.

### P1 — lint/문서 후속 (선택, 사용자와 합의)

4. `model/` 형제 파일 상한 · depth를 `RULES`에 추가.
5. 슬라이스명 kebab-case 강제.
6. shared / app / pages를 `structuredLayers`(또는 별도 RULES)로 점진 확장.
7. 두 lint 스크립트 통합.
8. legacy `widgets/header/ui/_parts` → **component-root** 마이그레이션 (`PortfolioHeader/_parts` 등) 후 allowlist 제거.

### 하지 말 것 (명시)

- 사용자 요청 없이 **git commit / push**.
- 레거시 allowlist에 **새 `ui/_parts` 경로 추가**.
- features/entities에 `_parts` 재도입.
- `_parts`를 index에 re-export.
- core 패키지에 앱 도메인 `SERVICE_KEY` 넣기 (기존 아키텍처 유지).

---

## 6. 지켜야 할 사항 (체크리스트)

### 6.1 FSD import

- [ ] 상위 레이어만 하위 import (Biome + 리뷰).
- [ ] widgets↔widgets, features↔features, entities↔entities **다른 슬라이스** 금지.
- [ ] widgets/features **내부**에서 `@FsdWidgets` / `@FsdFeatures` 쓰지 않기.
- [ ] `_parts`는 **`./_parts/...`만**.

### 6.2 `_parts` 배치

```
다른 widget·page에서도 쓰나?     → shared 또는 entities
독립 사용자 시나리오/액션인가?   → features
이 public widget 조립·레이아웃만? → widgets …/_parts (얇게)
_parts가 4~5개+ 시나리오 이름?   → features로 올리거나 widget 슬라이스 분리
```

### 6.3 폴더 깊이·파일 수

- ui 형제 `.tsx` 8+ → 폴더 분할.
- widgets ui depth from layer ≤ 5, features/entities ≤ 3 (수치 변경 시 **RULES + README 동시**).

### 6.4 레포 컨벤션

- 패키지: **pnpm**.
- 커밋: `[{type}/{name}] subject` — 예 `[feat/sbc] …`
- 브랜치: `{type}/{name}/#{issueNo}`
- Biome: indent 2, LF, single quotes(JS), `console.log` 금지.
- 응답/문서: 사용자 규칙상 **한국어**.
- AI 스킬: 웹 퍼블/FSD 작업 시 `node_modules/@shin96bc/ai-skills/.../SKILL.md` (+ reference) 읽을 것.

### 6.5 검증 명령

```bash
pnpm biome          # biome + 두 FSD lint
pnpm lint:fsd
pnpm lint:fsd:dry
pnpm typecheck
pnpm --filter @apps/user-portfolio run typecheck
```

Windows PowerShell에서는 `&&` 대신 `;` 사용에 유의.

---

## 7. 주요 파일 맵

```
README.md                          # 상세 규칙 (소스 오브 트루스)
AGENTS.md                          # 에이전트용 요약
SECURITY-NOTES.tmp.md              # 보안 설계 메모 (구현 대기, 임시)
biome.json                         # noRestrictedImports (_parts, widgets/features alias)
package.json                       # biome / lint:fsd 스크립트
scripts/lint-fsd-folder-structure.mjs   # RULES object ← 수치·정책 변경은 여기
scripts/lint-fsd-ui-private-folders.mjs # _parts 전용 가드 (중복 가능)
apps/user/portfolio/src/fsd/
  widgets/header/ui/_parts/        # LEGACY — 마이그레이션 대상
  widgets/portfolio/career/ui/…    # deep _parts import 제거된 쪽
  entities/content|site/…          # mock/lang/mapper 등 진행 중 diff 가능
```

Legacy allowlist (두 스크립트 **둘 다**에 동일 경로가 있어야 함):

```text
apps/user/portfolio/src/fsd/widgets/header/ui/_parts
```

---

## 8. 워킹트리에 있을 수 있는 변경 (참고)

대화 시작 시점 git status에 대략 다음이 포함되어 있었음 (이후 추가 수정 가능):

- `AGENTS.md`, `README.md`, `biome.json`, `package.json`
- `scripts/lint-fsd-folder-structure.mjs` (A)
- `scripts/lint-fsd-ui-private-folders.mjs` (A)
- content/site entities (api, mapper, mock ko/en/ja, `site-lang.ts` 등)
- `PortfolioCareer.tsx` 등 widgets

**커밋 전:** `git status` / `git diff`로 현재 스냅샷을 다시 보고, 사용자에게 커밋 단위(문서+lint vs content i18n vs 보안)를 나눌지 확인.

---

## 9. 새 AI를 위한 권장 착수 순서

1. 이 문서 §2·§6 읽기 + `README.md`의 `_parts` / Folder structure 절 읽기.
2. `pnpm lint:fsd:dry` + `pnpm biome`로 현재 통과 여부 확인.
3. 사용자에게 **P0 중 무엇부터**인지 확인 (기본 제안: entities same-slice 잔여 → header deep import → SECURITY).
4. 보안은 `SECURITY-NOTES.tmp.md`를 읽고 **구현 범위 브리핑** 후 코드 변경.
5. 정책 수치 바꿀 때: `RULES` + README/AGENTS + (필요 시) private-folders 상수 **함께** 수정.
6. 커밋은 **명시 요청 시에만**.

---

## 10. 열린 질문 / 합의 필요

- entities same-slice를 **Biome으로도 상대경로 강제**할지, 지금은 **컨벤션+일괄 변환만**인지 (현재는 후자).
- header `_parts` 레거시 마이그레이션을 P0에 넣을지, P1로 둘지.
- 두 FSD lint 스크립트 통합 시점.
- `SECURITY-NOTES.tmp.md`에서 **1차로 코드에 반영할 항목** 목록 (env 분리, 토큰 노출 완화, CSP 등).

---

## 11. 이 문서 자체

- 파일명: `#31작업중인 내용 정리.md` (루트).
- 작업 완료·커밋 후 **삭제하거나 `docs/`로 이동**해도 됨.
- 진행하면서 §3 Done / §5 TODO를 갱신하면 인수인계가 쉬워진다.
)
