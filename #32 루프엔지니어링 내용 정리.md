# #32 루프 엔지니어링 내용 정리

> **목적:** Next.js(이 모노레포)에 맞춘 **루프 엔지니어링**을 도입하기 위해, 합의·갭 분석·다음에 할 일을 한곳에 모은 문서다.  
> **범위:** [#31 인수인계](#31작업중인%20내용%20정리.md)에 **없는** 루프/에이전트 자율 검증 트랙만 다룬다. FSD `_parts`·Biome·header deep import·SECURITY 구현은 #31.  
> **소스 오브 트루스:** 앱/아키텍처 규칙은 [`README.md`](README.md) · [`AGENTS.md`](AGENTS.md). 충돌 시 README 우선.  
> **관련 대화:** Cursor — Next.js loop engineering gaps / #31 인수인계 연계 (2026-09)  
> **작성 시점:** 2026-09 · 이슈/브랜치 가정 `{type}/{name}/#32` (실제 브랜치는 생성 시 확정)

---

## 1. 한 줄 요약

이 레포는 **하네스(규칙·정적 검사·verify·smoke e2e·denylist 가드)** 까지는 갖춰져 있으나, AI가 **실행 → 검증 → 수정**을 목표 달성까지 반복하는 **루프 레이어**(BACKLOG/STATE/독립 verifier/budget)는 아직 없다.  
가장 큰 빈틈은 **루프 메모리(`BACKLOG` / `STATE` / `LOOP`)** 와 **독립 verifier·안전 레일(budget/kill switch)·Plan/Build/Report 루프 스킬**이다.  
스코프는 모노레포 전체를 한 번에 돌리지 말고 **`@apps/user-portfolio`부터**.

---

## 2. 배경 · 왜 필요한지 (의사결정)

### 2.1 루프 엔지니어링이란

- 프롬프트 한 번 → 사람이 검토 → 재지시 병목을 줄이고,  
  에이전트가 **목표(체크 가능한 acceptance)** 를 받을 때까지 **스스로 검증·수정**하게 만드는 설계.
- “더 나은 프롬프트”가 아니라 **반복 가능한 공장(게이트·메모리·격리·예산)** 을 짓는 일.

### 2.2 하네스 vs 루프 (이 레포 기준)

| 단계 | 이 레포에 이미 있는 것 | 루프에 추가로 필요한 것 |
|------|------------------------|-------------------------|
| 하네스 | `AGENTS.md`, FSD, Biome, `lint:fsd`, husky pre-push(typecheck+biome), MSW, `@shin96bc/ai-skills` | — |
| 루프 | (거의 없음) | verify 종료 조건, backlog/state, 독립 verifier, denylist/budget/lock, worktree, Plan/Build/Report |

#31에서 다룬 FSD lint 강화는 **하네스를 더 단단히** 하는 작업이지, 그 자체로 루프가 완성되지는 않는다. 루프는 #31 산출물(`pnpm biome` 등)을 **게이트의 일부**로 재사용한다.

### 2.3 Next.js + 이 모노레포에 맞출 때

- 단일 앱 starter(예: nextjs-loop-engineering-starter)와 달리 **앱이 여러 개** → verify/백로그/denylist를 **portfolio 스코프로 한정**.
- UI가 핵심 → typecheck만으로는 부족. **Playwright(결정적 e2e) + 스크린샷(`snap`) + 기존 MSW** 가 Next.js 맞춤 포인트.
- 인증/토큰/env는 #31 `SECURITY-NOTES.tmp.md`와 겹침 → 루프 denylist·human gate에 **보안 민감 경로**를 넣을 것 (구현 본문은 #31 P0-3).

### 2.4 참고 패턴 (외부)

- Plan → Build → Report, human merge only, `STATE.md` kill switch (`loop: paused`).
- Coding agent ≠ Verifier (자기 승인 금지).
- Git worktree(또는 Cursor cloud worktree)로 메인과 격리.
- 예산·시도 횟수·concurrency lock으로 무한 루프·토큰 폭주 방지.

공식으로 이 레포에 복제할 의무는 없음. **개념·레일만** 차용하고 경로·스크립트는 우리 스택(pnpm, turbo, Jest→필요 시 유지, Biome, FSD)에 맞춘다.

---

## 3. 갭 분석 (현재 vs 루프 레디)

### 3.1 이미 있는 것 ✅

| 자산 | 위치/메모 |
|------|-----------|
| 에이전트 규칙 요약 | `AGENTS.md` (`verify:portfolio` / `guard:harness` 포함) |
| 정적 품질 | `pnpm typecheck`, `pnpm biome` (+ FSD folder lints) |
| pre-push | typecheck + biome |
| MSW | `apps/user/portfolio` mock handlers — e2e/snap 시드의 씨앗 |
| AI skills (퍼블/FSD/Jest) | `node_modules/@shin96bc/ai-skills/...` |
| 배포 워크플로 | `.github/workflows/deploy-*.yml` (quality = `verify:portfolio`) |
| 통합 `verify` | `pnpm verify:portfolio` (typecheck+biome+unit+guard+e2e+build) |
| 단위 테스트 씨앗 | portfolio Jest + mapper specs |
| e2e smoke | Playwright `apps/user/portfolio/e2e/` (MSW, locale 고정) |
| denylist / secret 가드 | `docs/harness/DENYLIST.md` + `pnpm guard:harness` |
| 하네스 스킬 | `.cursor/skills/verify` · `plan-change` · `safe-edit` |
| PR verify CI | `.github/workflows/verify-portfolio.yml` |
| 하네스 문서 | `docs/harness/README.md` |

### 3.2 없는 것 / 약한 것 ❌ (루프 레이어)

| 레일 | 현황 |
|------|------|
| 에이전트 vision (`snap`) | 없음 (H4 이후 / 루프 L3) |
| 루프 메모리 | `BACKLOG.md` / `STATE.md` / `LOOP.md` / run-log 없음 |
| 독립 verifier | 없음 |
| budget / kill switch / concurrency lock | 없음 (`guard`는 denylist·secret만) |
| worktree 루프 절차 | 문서·스크립트 없음 |
| Plan/Build/Report **루프** 스킬 | 하네스 스킬만 있음 — 자율 Report/Build 루프 아님 |

---

## 4. 목표 아키텍처 (도입 후 그림)

```text
사람: BACKLOG에 체크 가능한 acceptance 작성 (또는 Plan 모드)
        │
        ▼
Report 루프: PR/CI/백로그 관측 → STATE.md 갱신 (코드 변경 금지)
        │  (신뢰 쌓인 뒤)
        ▼
Build 루프: lock → worktree/branch → 구현+테스트 → snap/ui-verify
        → 독립 verifier(APPROVE) → PR 오픈 (auto-merge 금지)
        │
        ▼
사람: PR 리뷰·머지 → BACKLOG-DONE → backlog 재충전
```

Emergency: `STATE.md`에 `loop: paused` → 모든 루프/관련 CI 중단.

---

## 5. 다음에 해야 할 작업 (우선순위)

#31과 **병행 가능**하되, 브랜치/커밋은 사용자와 나눈다. 기본 제안: **#31 P0 마무리 후** 또는 **별 이슈 #32 브랜치**.

### L0 — 최소 게이트 (가장 먼저) — **Done (H3)**

1. **`pnpm verify:portfolio`** — typecheck + biome/fsd + unit + guard + e2e + build
2. AGENTS.md / `docs/harness`에 **루프·에이전트가 돌릴 단일 명령**으로 명시

### L1 — 테스트 씨앗 — **Done (H3)**

3. portfolio Jest 스펙 (mapper) 추가됨
4. acceptance에 “테스트 통과”를 적을 수 있음

### L2 — 루프 메모리 + Report-only

5. 루트(또는 `docs/loop/`)에:
   - `BACKLOG.md` — 템플릿 + 체크 가능한 AC
   - `STATE.md` — `loop: running|paused`, WIP, Waiting on Human
   - `LOOP.md` — limits, denylist, human gates, active loops
   - `loop-run-log.md` — 시도/실패 이력
6. **Report 모드만** 먼저 (~관측 신뢰 후 Build)
7. kill switch: `loop: paused`

> **참고:** 하네스 denylist는 `docs/harness/DENYLIST.md`에 있음. 루프 `LOOP.md` 도입 시 단일 소스로 통합·참조할 것.

### L3 — Next.js UI 검증 — **부분 Done (H4 smoke)**

8. Playwright + **viewport/locale/timezone 고정** — smoke 있음
9. MSW로 **결정적** e2e — smoke 있음
10. `pnpm snap <route>` → 스크린샷 ui-verify — **미도입**
11. `verify:portfolio`에 e2e 편입됨 (snap 가드는 미도입)

### L4 — 안전·독립성 — **부분 Done (H5 가드)**

12. Coding ≠ Verifier (별 에이전트/스크립트, APPROVE/REJECT, reject 상한 후 에스컬레이션) — **미도입**
13. denylist — `docs/harness/DENYLIST.md` + `guard-harness` (**Done**). 루프 시 `LOOP.md`로 승격·정렬
14. budget / 시도 횟수 / concurrency lock — **미도입**
15. worktree(또는 cloud worktree) 절차 문서화 — **미도입**
16. human merge only — 브랜치 보호·auto-merge 금지 (설정+문서) — **미도입**

### L5 — 워크플로 스킬 + CI — **부분 Done (H6)**

17. Plan / Build / Report **루프** 스킬 — 미도입. 하네스 스킬(`.cursor/skills/verify|plan-change|safe-edit`)은 **Done**
18. PR CI = `verify:portfolio` 미러 — **Done** (`.github/workflows/verify-portfolio.yml`)
19. (선택) 일일/주기 Report는 Cursor `/loop` 또는 동등 스케줄 — **미도입**

### 하지 말 것 (명시)

- 사용자 요청 없이 **git commit / push / force push**.
- 백로그 없이 / AC 모호한 채로 **Build 루프** 돌리기.
- verifier 없이 coding 에이전트 **자기 APPROVE**.
- denylist·paused 없이 **무제한 토큰 루프**.
- 모노레포 전 앱을 한 verify에 묶어 첫날부터 돌리기 (portfolio 스코프 먼저).
- 시크릿·`.env` 실값 커밋.
- #31 레거시 `ui/_parts` allowlist에 경로 추가 등 **FSD 합의 위반** (루프가 만들어도 reject).

---

## 6. #31과의 관계

| 항목 | 트랙 |
|------|------|
| FSD `_parts`, Biome alias, lint:fsd, header public API, entities 상대경로 | **#31** |
| SECURITY-NOTES → 토큰/env/CSP 코드 반영 | **#31 P0-3** (루프 denylist·human gate에 **경로만** 연동) |
| verify / 테스트 / e2e / snap / BACKLOG·STATE·LOOP / verifier / 스킬 | **#32 (이 문서)** |

루프 Build가 켜지면 #31 규칙(`pnpm biome`, public API only)은 **verifier 입력**이 된다. #31이 흔들리면 루프도 흔들린다 → P0 FSD/보안 정리는 루프 품질의 전제.

---

## 7. 지켜야 할 사항 (체크리스트)

- [ ] 루프 종료 조건은 **사람 눈**이 아니라 `verify`(+ 테스트/e2e)로 말할 수 있는가.
- [ ] BACKLOG AC는 **체크 가능**한가 (`pnpm snap /…` 등 UI면 시각 검증 한 줄 포함).
- [ ] 한 Build 실행 = **백로그 항목 하나**.
- [ ] Verifier는 coding과 **분리**, REJECT 누적 시 STATE에 에스컬레이션 후 정지.
- [ ] `loop: paused`면 즉시 정지.
- [ ] 변경은 worktree/feature 브랜치 → **PR만**, 머지는 사람.
- [ ] denylist·limits의 단일 소스는 `LOOP.md` (훅/CI/verifier가 같은 목록 파싱).
- [ ] 패키지 **pnpm**, 응답/문서 **한국어**, 커밋 컨벤션은 AGENTS/README.

### 검증 명령 (현행 + 목표)

```bash
# 현행 하네스 게이트 (H3~H6)
pnpm verify:portfolio
pnpm guard:harness

# 부분 실행
pnpm biome
pnpm lint:fsd
pnpm typecheck
pnpm --filter @apps/user-portfolio run typecheck
pnpm --filter @apps/user-portfolio run test
pnpm --filter @apps/user-portfolio run test:e2e
```

Windows PowerShell에서는 `&&` 대신 `;` 사용에 유의.

---

## 8. 주요 파일 맵 (목표)

```
#32 루프엔지니어링 내용 정리.md   # 이 문서 (인수인계)
AGENTS.md                         # verify:portfolio · 루프 요약 링크 (추후)
BACKLOG.md                        # 목표 — 작업 계약
STATE.md                          # 목표 — paused / WIP / escalations
LOOP.md                           # 목표 — limits, denylist, human gates
loop-run-log.md                   # 목표 — 실행 이력
BACKLOG-DONE.md                   # 목표 — 완료 요약
scripts/                          # 목표 — verify, lock, snap, run-verifier
.cursor/skills/ 또는 동등         # 목표 — plan / build / report
apps/user/portfolio/
  (기존) MSW mock                 # e2e/snap deterministic seed에 연결
  (목표) e2e/, snap 산출물
.github/workflows/                # 목표 — PR verify (deploy와 분리)
```

Framework vs product: 루프 md/스크립트/스킬은 **유지·적응**, 화면/도메인 코드는 기존 FSD 제품.

---

## 9. 권장 착수 순서 (새 AI / 사람)

1. 이 문서 §2·§5·§6 + `AGENTS.md` Commands 절 읽기.
2. #31 문서 §5 P0 잔여가 있으면 **사용자에게 병행/선후** 확인.
3. **L0 `verify:portfolio`** 부터 — 테스트 없이라도 typecheck+biome+build로 “한 방 명령” 만들기.
4. L1 테스트 씨앗 → L2 BACKLOG/STATE/LOOP + **Report-only**.
5. Build는 Report 신뢰 후, AC가 있는 항목만.
6. 정책·수치 변경 시: `LOOP.md` + AGENTS + (있다면) CI/훅 **함께**.
7. 커밋은 **명시 요청 시에만**. 커밋 단위 예: `chore` verify 스크립트 / `test` 씨앗 / `docs` 루프 md / `feat` e2e.

---

## 10. 열린 질문 / 합의 필요

- 루프 인프라를 **#31 브랜치에 섞을지**, **`feat|chore/.../#32` 별 브랜치**로 할지.
- 단위 테스트 러너: 기존 **Jest 유지** vs Vitest 등 전환 (루프 starter와 맞출지).
- e2e 데이터를 **MSW만**으로 갈지, 서버/시드 DB가 필요한 시나리오가 있는지.
- denylist 1차 목록에 넣을 경로 (auth, cookie crypto, env, next-auth callbacks 등 — SECURITY-NOTES와 정렬).
- Report를 로컬 `/loop`로 돌릴지, CI 스케줄만으로 할지.
- `BACKLOG*.md` / `STATE.md`를 루트에 둘지 `docs/loop/`에 둘지.

---

## 11. 이 문서 자체

- 파일명: `#32 루프엔지니어링 내용 정리.md` (루트).
- L0~L2 착수·커밋 후 **§3/§5를 갱신**하거나, 안정화되면 `docs/loop/`로 이동·정리해도 됨.
- #31 문서와 역할이 겹치지 않게: **FSD/보안 구현 상세는 #31**, **루프 레일·verify·백로그는 #32**.
